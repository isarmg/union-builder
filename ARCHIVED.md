# Archived

The Union composition builder is being retired in favor of per-product release tooling.
Only the generic release-packaging ideas (manifest, checksums, SBOM, signing, install/rollback)
should move to `isarmg-foundation`; module profiles and dependency graphs must not be preserved.
